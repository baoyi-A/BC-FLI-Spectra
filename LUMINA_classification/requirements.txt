# LUMINA -- dual-anchor barcode classification network.
# Install PyTorch FIRST, following https://pytorch.org/get-started/locally/ for your
# CUDA version (torch/torchvision are deliberately not pinned here so this file does
# not overwrite a working CUDA build).
numpy>=1.23,<2
scipy>=1.10
pandas>=2.0
# pandas reads and writes .xlsx through openpyxl but does not itself depend on it.
# Train_LUMINA.py reads clustered.xlsx with pd.read_excel and names the engine outright
# in its pd.ExcelWriter(..., engine='openpyxl') calls; Test_LUMINA.py writes
# predict_class_*.xlsx; Visualize_heatmap.py reads those two back; and
# Finetune_LUMINA.py's "--labels auto" reads clustered.xlsx when a sample folder has
# one. Without this the failure is an ImportError raised from inside pandas, after a
# long scan has already run.
openpyxl>=3.1
scikit-learn>=1.2
opencv-python>=4.10
tifffile>=2021.7
PyWavelets>=1.4
matplotlib>=3.7
seaborn>=0.13
tqdm>=4.66
